// Module included in the following assemblies:
//
// *mcp_gateway_config/mcp-gateway-observe.adoc

:_mod-docs-content-type: REFERENCE
[id="ref-mcp-gateway-otel-logs-export_{context}"]
= Logs export through {OTELName}

[role="_abstract"]
After telemetry initialization, `slog` log lines from the {mcpg} are exported through the OpenTelemetry protocol (OTLP) endpoint in addition to standard output (`stdout`). Logs emitted before telemetry initialization remain on stdout only. Logs emitted within a traced request include trace context, which enables backends such as Grafana to link log entries to their corresponding trace spans.

This correlation means that you can jump from a span in the trace timeline directly to the relevant log entries without manual correlation.
